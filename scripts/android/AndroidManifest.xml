<?xml version="1.0" encoding="utf-8"?>
<!--
  Minimal NativeActivity manifest for an Azul app on Android.
  No Java/Kotlin source code is required for Phase 1-4 — the entire
  application is the Rust cdylib placed under lib/<abi>/lib${APP_NAME}.so.

  Substitute via sed in build-android.sh:
    @PACKAGE@   — reverse-DNS bundle id (e.g. com.azul.example)
    @LABEL@     — user-visible app name
    @LIB_NAME@  — the .so basename minus "lib" prefix and ".so" suffix
                  (e.g. for libazul.so the lib_name is "azul")
    @VERSION_CODE@ / @VERSION_NAME@
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="@PACKAGE@"
    android:versionCode="@VERSION_CODE@"
    android:versionName="@VERSION_NAME@">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34" />

    <!-- Normal permissions: granted at install, no runtime prompt, and needed
         by so much of the engine (fonts, tiles, updates, the debug server)
         that withholding them by default helps nobody. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- DANGEROUS permissions are opt-in, injected here by build-android.sh
         from AZ_ANDROID_PERMISSIONS. Declaring CAMERA / RECORD_AUDIO /
         ACCESS_FINE_LOCATION for every azul app would be least-privilege
         backwards: a text editor asking for the microphone is a review
         rejection and a broken promise to the user, and the runtime prompt
         still gates them anyway. An app that wants one asks for it:

           AZ_ANDROID_PERMISSIONS="android.permission.CAMERA,android.permission.ACCESS_FINE_LOCATION" \
             bash scripts/build-android.sh ...
    -->
@EXTRA_PERMISSIONS@

    <application
        android:label="@LABEL@"
        android:hasCode="false">

        <activity
            android:name="com.azul.app.AzulActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|density|uiMode"
            android:exported="true">

            <meta-data
                android:name="android.app.lib_name"
                android:value="@LIB_NAME@" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
