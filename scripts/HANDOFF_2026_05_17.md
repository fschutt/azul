# Azul Bindings — Handoff 2026-05-17 (session end)

**Branch:** `layout-debug-clean`
**Session ended:** 2026-05-16 ~03:50 (after 27 commits since HANDOFF_2026_05_16).
**Last commit:** `29392eeab` (HANDOFF item 18 closed marker).
**Loop state:** PAUSED — binding queue exhausted; WB1.x work needs user-attended planning.

## What this session shipped

All 18 items from `HANDOFF_2026_05_16.md` complete, in order:

| # | Item | Commits |
|---|---|---|
| 1 | CC-4 Node `.with(opts)` mirror | `070a3c946` |
| 2 | CC-4 Lua `:with(opts)` mirror (surfaced pre-existing `cdata == nil` SIGSEGV) | `9125d6c53` |
| 3 | CC-6 Lua void-mutator → return self for chaining | `76b804213` |
| 4 | CC-6 OCaml `|>` pipeline hello-world rewrite | `04948da25` |
| 5 | A.7.6 Node hello-world trim (99 → 60 LOC) | `70e052b11` |
| 6 | V9 Python Vec iter audit (no sequence protocol) | `5a650d703` |
| 7 | Haskell `consume<X>` per-method investigation → re-queued as item 18 | `e80570148` + `6b53df410` |
| 8 | E2E test runner Java/Kotlin/C# hooks | `2bc20c57f` |
| 9 | V7 OCaml `to_list` Vec iterator (52 modules) | `bd8a7b71c` |
| 10 | Java/Kotlin/Scala `String → AzulString` rename | `af6855e4e` |
| 11 | CC-1 Java typed `Data<T>` SAMs (17/19 kinds) | `533df7ab5` |
| 12 | CC-1 Kotlin mirror | `aadcf3a01` |
| 13 | CC-1 C# mirror | `ccb59cb60` |
| 14 | V8 Haskell Vec iter clone-via | `e73ab429c` |
| 15 | V7.2 OCaml primitive `to_array` (5 modules) | `84cfca5cb` |
| 16 | OCaml I.5.6 Option/Result `intoSome`/`intoOk`/`intoErr` (138 emits) | `d350b0fa9` |
| 17 | Per-binding READMEs refresh (16 files) | `83ccf9f5b` |
| 18 (was 7) | Haskell per-method emit, Phases 1-6 (2269 wrappers) | `350d45db6` `16c40aaa4` `09b670ab9` `4e430b5d9` `3d09f2304` `15ffd2053` `29392eeab` |

27 commits, all on `layout-debug-clean`. Build verification (per loop hygiene):
- `cargo run --bin azul-doc -- codegen` — clean every iteration.
- `mvn -q package` (examples/java) — exit 0.
- `kotlinc Azul.kt HelloWorld.kt -include-runtime -d hello-world.jar` — exit 0.
- `dune build` (examples/ocaml) — exit 0.
- `cabal build` (examples/haskell) — exit 0.
- `dotnet build` (examples/csharp) — exit 0.
- `node --check`, `luajit -bl`, `ruby -c` — all syntax OK.

## Pre-existing bugs surfaced (out-of-scope, flagged)

1. **Lua `__eq` SIGSEGV on `cdata == nil`** — Phase I.2.7's `__eq` metamethod passes the nil RHS straight to `_partialEq`, which dereferences NULL. Worked around in CC-4 Lua's `_apply_opts` via `if not opts then return end`. Fix is a `type(b) ~= 'cdata'` guard in the metamethod. Flagged in `memory/memory_safety_session_2026_05_15.md`.
2. **Scala `Dom.getPointer` vs `Dom.rawPointer`** — pre-existing naming mismatch in `examples/scala/HelloWorld.scala`; scalac errors. Not caused by the `String → AzulString` rename. Not in scope for any session item.
3. **C# `OptionString` codegen mismatch** — `examples/csharp/Azul.cs` (and Java mirror) `len(&self, dom_vec: AzDomVec)` method signature carries an extraneous self-copy arg. Pre-existing, flagged in the V9 audit memory.

## What's queued for next session

Two large arcs ready to start:

### Arc A: Haskell per-method Phases 7+

Per `memory/haskell_consume_per_method_investigation_2026_05_16.md`. Six phases done (2269 wrappers); remaining work covers ARG shapes:

  - **Phase 7** String args: needs `azStringFromString :: String -> IO T.AzString` helper in Types.hs (UTF-8 encoder via `Data.ByteString.UTF8` or `Data.Text.Encoding`). ~1h.
  - **Phase 8** Vec args: needs `mk<X>VecFromList :: [Elem] -> IO T.<X>Vec` helper per Vec wrapper. Requires malloc + element marshalling. ~2h per shape.
  - **Phase 9** Option<T> args: encoder from `Maybe T` to `T.Option<X>` (tag-byte + payload poke at alignment). ~2h.
  - **Phase 10** Result<T,E> args: dual-arm Either→Result encoder. ~2h.
  - **Phase 11** ForeignPtr refactor: rework `mkFoo`/`withFoo`/`disposeFoo` so returns can be `IO Foo` (managed) instead of `IO T.Foo` (raw). Would let users skip the manual `mkFoo` after every clone/get-Y call. ~3h.
  - **Phase 12** Callback args via `register<X>Callback` integration: pass `(MyData -> Info -> IO Dom)` directly. ~2h.

Total Phase 7-12: ~13h. Each phase independent + commit-safe.

### Arc B: Web backend / remill continuation

Per `web.md` and HANDOFF_2026_05_16 WB1.x → WB4.x. **HEADS UP — the dll/src/web/ layout drifted since HANDOFF_2026_05_16 was written:**

  - HANDOFF says "wire into `dll/src/web/mini_gen.rs`" — that file doesn't exist.
  - Current layout: `dll/src/web/transpiler.rs` (`Transpiler` trait + `StubTranspiler` always-fails), `dll/src/web/transpiler_remill.rs` (scaffolded `RemillTranspiler`, both methods `todo!()`), `dll/src/web/classify.rs` / `loader_js.rs` / `server.rs` / `html_render.rs` / `mod.rs::generate_mini_wasm` (8-byte WASM stub).
  - **Updated WB1.1**: wire `experiments/transpile-blueprint`'s remill+LLVM pipeline into `transpiler_remill.rs::lift_function` (NOT `mini_gen.rs`). The integration point is the `web-transpiler` Cargo feature gate.
  - Pre-requisite: `third_party/remill-rs` submodule (referenced in transpiler_remill.rs comments but not yet committed). Submodule setup is itself a 1-2h task.

The WB arc needs user-attended planning because:
  - Multi-day scope (remill toolchain + LLVM + wasm-ld setup).
  - Branch-blocking nature (touching `dll/src/web/` ripples into the build matrix).
  - HANDOFF references are stale (file names + module layout).

## Recommended next session start

User triage decision needed before resuming:

  1. Continue Haskell per-method Phases 7+ (low-risk, ergonomics-only).
  2. Start WB1.1 with submodule setup (multi-day, blocks other web work).
  3. Pivot to something else entirely (libazul-side blockers, new feature, etc.).

If (1): the loop directive can re-engage with the same 60s cadence; each
phase is well-scoped and lands independently. Plan in
`memory/haskell_consume_per_method_investigation_2026_05_16.md`.

If (2): user-attended pairing on the submodule + LLVM toolchain
setup before the loop can do meaningful per-iteration work. The
experiment's `Cargo.toml` already documents the `REMILL_INSTALL_DIR`
env-var dance; the C++ shim build needs a host-LLVM that matches
remill's expected version.

If (3): see `memory/MEMORY.md` for the current state of every
parked arc.

## Loop hygiene rules (unchanged)

- 60-second `<<autonomous-loop-dynamic>>` cadence (per user direction).
- No force-pushes, no `branch -D`, no `reset --hard`. Every commit scoped + cherry-pickable.
- Each commit cites the audit/plan/item reference for traceability.
- Stall behavior: commit safely, update relevant memory with the blocker description + code refs, move on.

## Memory entries updated this session

- `loop_directive_2026_05_13.md` — cadence dropped from 15min → 60s.
- `memory_safety_session_2026_05_15.md` — added the Lua `__eq` SIGSEGV note.
- `haskell_consume_per_method_investigation_2026_05_16.md` — re-queued from deferred-closure to QUEUED, then to PHASES 1-6 LANDED with full status.
- `MEMORY.md` — index entry for the Haskell investigation memory.

---

End of handoff. 27 commits this session; loop paused.
