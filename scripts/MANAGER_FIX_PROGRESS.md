# Manager-wiring fix arc — progress + control file

STATUS: ACTIVE (phases A → B → C, then D compile gate)
Branch: `fix/manager-wiring` (NO pushes; merge decision is the user's)
Checklist source: `scripts/MANAGER_WIRING_AUDIT_2026_07_03.md` (audited @30162fa9f — line numbers drift, ALWAYS re-locate by symbol)
Raw audit JSON (deep detail, may not survive reboot): `/private/tmp/claude-501/-Users-fschutt-Development-azul-mobile/896d2157-9818-4f1d-b5df-2f21b1ce94a9/tasks/wkn87j5an.output`; per-agent journal: `/Users/fschutt/.claude/projects/-Users-fschutt-Development-azul-mobile/896d2157-9818-4f1d-b5df-2f21b1ce94a9/subagents/workflows/wf_47ed3a06-26b/journal.jsonl`

## Rules (binding for every tick)
1. NO compilation in phases A–C: no cargo build/check/test/run, no azul-doc, no rustc. Code + tests + rg/Read only. Phase D lifts this.
2. Re-locate every fix site by SYMBOL from the audit, not line number. If the code materially changed or is already fixed → mark item OBSOLETE + note, move on.
3. Fix each item across ALL backends it names before taking the next item.
4. Tests: unit tests for platform-independent logic (layout managers, core event logic). Headless E2E only for paths headless actually supports (land MWA-A4 first). Never write a test that cannot work without an OS; record test-debt in the item note instead.
5. Public API changes: edit api.json ONLY (never touch generated bindings, never run generators now); record under API-STAGED. Codegen via the azul-doc autofix workflow happens in Phase D (memory rule: autofix only, no hand-curated patches).
6. Commit per item: `fix(<area>): <summary> [<item-id>]` ending with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Update this file's item status + short sha in the same or an immediate follow-up commit. Touch `scripts/.manager_fix.lock` after each commit (heartbeat).
7. Never push. Never commit on master.
8. Blocked? Mark BLOCKED + one-line question here; continue with the next item. DECISIONS below are standing answers — do not re-ask them.
9. Behavior you cannot runtime-verify without compiling (esp. Wayland/X11/Windows): implement per protocol/API docs and tag the item NEEDS-RUNTIME-VERIFY; Phase D's smoke list picks these up.
10. Lock protocol: on start, if `scripts/.manager_fix.lock` exists with mtime < 45 min → another tick is active, exit silently. Else write timestamp+pid to it. Remove it on clean exit.
11. NO NEW THREADS anywhere in this arc (user, twice): single-threaded designs only — recurring shell timers are the wake/poll mechanism (WASM constraint). Async/capability drains do NOT belong in regenerate_layout ("they have nothing to do with layout") — they live in the event-pass path.

## DECISIONS (CONFIRMED by user 2026-07-03 except where marked)
- D1 SCOPE — FIX EVERYTHING. Real platform backends ARE in scope (macOS CoreLocation, Windows WinRT Geolocator + Windows Hello/UserConsentVerifier, Linux xdg-desktop-portal permissions + GeoClue2 + fprintd, ...). Phase C implements the report's fix sketches fully; only FOLLOW-UPS below are out. [CONFIRMED]
- D2 macOS titlebar — `performWindowDragWithEvent:` (OS-smooth) from the titlebar node's mouse-down; manual-math path FIXED anyway as fallback/programmatic move (Y-flip uses screen frame origin; fractional residual carried; restore-before-drag when maximized; no 5px threshold for window-drag). [CONFIRMED]
- D3 Capability pump — TIMER-ARMED and SINGLE-THREADED (NO pump thread): while any capability subscription/use is active, arm a recurring shell timer (gamepad ~16ms; slower per-subsystem cadences where sensible, e.g. sensors 50-100ms) that drains sources → synthesizes manager events → requests an event pass. Rationale (user): must run on WASM where there are no threads. Backend timers must wake blocked loops on all shells (they already drive user timers — reuse that path); drain moves from regenerate_layout into early process_window_events. [CONFIRMED — replaces the earlier thread design]
- D4 Shortcuts — primary modifier (Cmd on macOS, Ctrl elsewhere) in core keyboard state; `KeyboardShortcut::from_key` keys off primary; Windows normalizes generic VK_CONTROL/VK_SHIFT/VK_MENU. PLUS full native macOS Edit menu with key equivalents (MWA-B14). [CONFIRMED]
- D5 API — BREAKING api.json changes allowed. Stage edits during A–C under API-STAGED; Phase D runs azul-doc autofix + `azul-doc codegen all` to regenerate ALL language bindings. [CONFIRMED]
- D6 End game — fully automatic. SUCCESS CONDITION: entire audit list done (every report §5 gap FIXED / OBSOLETE / FOLLOW-UP) AND azul-dll (--features build-dll) AND examples compile. Compilation only at the end, for speed. [CONFIRMED]
- D7 Ordering — per-subsystem across all backends at once; verify at the end with compile + cross-compile (see Phase D). [CONFIRMED]
- D8 a11y — `a11y` JOINS azul-dll DEFAULT features (users can opt out). Implemented as MWA-A3f. [CONFIRMED]
- D9 Dependency policy for new platform backends — match repo style: dlopen + hand-rolled bindings. Linux portals/GeoClue2/fprintd via the existing linux/dbus + shared_dbus infra; WinRT via combase dlopen + RoGetActivationFactory/HSTRING plumbing; macOS via objc2/dlopen frameworks. NO new heavy crates (no windows-rs, no zbus/ashpd, no tokio). [DEFAULT — user may veto]
- D10 Mobile/web — audited surface = 4 desktops + headless. Default: iOS/Android real backends (GCController etc.) and dll/src/web wiring stay FOLLOW-UPS; mobile capability reports made HONEST now; all new designs (esp. D3 timer pump) must remain WASM/single-thread-compatible. [OPEN — awaiting user]

## Phase A — architecture first (in order)
- [x] MWA-A1a Capability pump core (pattern 1) — DONE (this commit). Landed: NEW dll/src/desktop/shell2/common/capability_pump.rs (pump() = all six drains + listener-gated gilrs/sensor ensure+poll + request dispatch; desired_interval_ms(); make_pump_timer(); inert marker callback); regenerate_layout stripped of steps 7a/7c-7f/7h/7i (keeps DOM-derived diffs + NEW listener-flag walk for GamepadInput/SensorChanged callbacks); CAPABILITY_PUMP_TIMER_ID=0x0005 (core/task.rs); pump + sync_capability_pump_timer at top of process_window_events (depth 0) + re-sync at end of pass; invoke_expired_timers fires an event pass when the pump timer expired; EventType gained GeolocationFix/GeolocationError variants + hover/window filter match arms + event_type_to_filters arms; GeolocationManager is now an EventProvider (pending-fix flag, clear after pass, has_active_subscription) and is registered in the provider list; gamepad/sensor managers got has_listeners arming flags; iOS/Android gamepad capability report made honest (supported=false until real backends). Unit tests: geolocation provider emit/clear + subscription flag, listener flags both managers. TEST-DEBT: pump()/sync timer need a headless E2E once MWA-A4 lands.
- [x] MWA-A1b Capability pump completion — DONE (this commit). EventType/HoverEventFilter/WindowEventFilter gained PermissionChanged/BiometricResult/KeyringResult (appended at enum ends; all match/filter/mapping arms incl. the exhaustive to_focus_event_filter / to_hover_event_filter); EventProvider impls: PermissionManager (one event per state flip, TARGETED at the capability's last_subscriber node via new pending_changed queue; has_pending_async = any state Requested), BiometricManager + KeyringManager (event on EVERY completion incl. identical repeats — a repeat answers a fresh prompt; in_flight counters: mark_requests_dispatched +n / set_last_result −1 saturating); geolocation error channel (LocationError layout-internal type, push/drain_location_errors, last_error cleared by next changed fix, GeolocationError provider event); pump dispatches requests with in-flight marking and treats every drained result as changed; desired_interval_ms adds ASYNC_RESULT_INTERVAL_MS=100 while any of permission/biometric/keyring has pending async; providers list is now 7 managers; clear block covers all. Unit tests: targeted permission event + Requested arming, biometric in-flight lifecycle + repeat-outcome events, keyring op lifecycle, geolocation error channel/provider. STATUS: DONE
- [x] MWA-A2 Primary-modifier + Windows VK normalization (pattern 2) — DONE (this commit). KeyboardState::primary_down() (Cmd/super on macOS via cfg!, Ctrl elsewhere); KeyboardShortcut::from_key keys off primary (param renamed, doc'd); handle_key_down: shortcut block + Ctrl/Cmd+D use primary, word-jump/word-delete use the platform word modifier (Option on macOS, Ctrl elsewhere — fixes Option-arrow/Option-Backspace too); win_event.rs maps generic VK_SHIFT/VK_CONTROL/VK_MENU → L-variants (WM_KEYDOWN delivers generics; they were dropped → ctrl_down() never true on Windows). macOS Cmd→LWin keycode 0x37 confirmed with existing unit test. Unit tests: from_key primary matrix, primary_down platform semantics. NOTE for Phase D smoke: verify macOS flagsChanged actually folds LWin into pressed_virtual_keycodes at runtime (B14 Edit menu is the belt-and-braces path). STATUS: DONE
- [x] MWA-A3 Output-drain sweep — DONE (this commit). a) Windows raise(): update_if_active's QueuedEvents now raised (was `let _ =` — zero UIA events ever fired incl. live regions); Windows set_focus no-op verified CORRECT (SubclassingAdapter observes WM_SETFOCUS itself) and re-documented. b) unix set_focus now calls adapter.update_window_focus_state (old "automatically managed" comment was false); wired from X11 FocusIn/FocusOut + wayland keyboard enter/leave (shared Linux adapter). c) apply_hover_restyle + per-DOM MouseEnter/MouseLeave collection in process_window_events → incremental :hover restyle with granular classification (paint-only vs incremental relayout); :active restyle deferred to MWA-C-hover (needs pressed-node tracking). d) macOS windowDidBecomeKey/ResignKey: snapshot-before-mutate + immediate event pass via new apply_activation_pass_result (X11/Windows WM_SETFOCUS/KILLFOCUS/Wayland were ALREADY fixed post-audit — that part OBSOLETE). e) new PlatformWindow::flush_a11y_tree_update (default no-op; overridden on 4 desktop backends) called at end of every top-level pass → incremental text-edit a11y updates reach AT immediately; windows/x11 relayout push switched clone→take for consistency. f) OBSOLETE — `a11y` is already in dll default features (Cargo.toml default list). TEST-DEBT: hover-restyle E2E after MWA-A4. STATUS: DONE
  Original sub-item descriptions follow for reference:
  - a) Windows a11y: capture and `.raise()` the QueuedEvents returned by `update_if_active` (windows/accessibility.rs, currently `let _ =`); implement `set_focus` (currently `_has_focus` no-op).
  - b) X11/Wayland a11y: call `update_window_focus_state(true/false)` from X11 FocusIn/FocusOut and wl_keyboard enter/leave (x11/accessibility.rs set_focus stub has a false "automatically managed" comment).
  - c) hover: feed HoverChange into `restyle_on_state_change` (common/event.rs passes `None, // hover`) from the MouseEnter/Leave diff → incremental :hover restyle on every backend.
  - d) window activation: snapshot previous_window_state, flip window_focused, run process_window_events in macOS windowDidBecomeKey/ResignKey, X11 focus handlers, Windows WM_ACTIVATE, wayland keyboard enter/leave → WindowFocusIn/Out finally dispatch; blur pauses caret + dims selection (focus_cursor tie-in).
  - e) a11y incremental text updates: push the computed update (layout/window.rs, text-edit tree-update fn) to the active platform adapter (+ raise on Windows).
  - f) flip `a11y` into azul-dll DEFAULT features (Cargo.toml edit only; per D8).
  STATUS: TODO
- [x] MWA-A4 Headless input parity — DONE (this commit). record_headless_input() helper feeds gesture input sessions from MouseMove/Down/Up in BOTH headless dispatch sites (run loop + test-harness step fn) — drags/double-clicks/long-press/swipes/node-DnD are now observable headlessly. NEW HeadlessEvent::{FileHover, FileDrop, FileHoverCancel} mirror the OS DnD ingress (position + hit test + FileDropManager + event pass + OS-style post-pass cleanup). Wheel + scroll easing: OBSOLETE — headless Scroll already drives record_scroll_from_hit_test + SCROLL_MOMENTUM timer, and the run loop ticks process_timers_and_threads (fixed post-audit). TEST-DEBT: gesture E2Es themselves land with the B-items they verify (B4/B7/B8/B12). STATUS: DONE

## Phase B — confirmed big-ticket bugs (after A)
- [x] MWA-B1 Horizontal wheel — DONE (this commit). Windows: OBSOLETE — WM_MOUSEHWHEEL already handled post-audit (combined WM_MOUSEWHEEL|WM_MOUSEHWHEEL arm routes horizontal into record_scroll_from_hit_test). X11: buttons 6/7 now map to handle_scroll(±1, 0) matching the 4/5 vertical sign convention (NEEDS-RUNTIME-VERIFY: hw sign check in Phase D smoke). STATUS: DONE
- [x] MWA-B2 Nested scroll — DONE (this commit). record_scroll_from_hit_test selects innermost-first (keys().rev(); ancestors have lower arena ids) with BOUNDARY HANDOFF: new select_scroll_target + can_consume_delta test remaining travel against the direction-normalized delta (post scroll_sign(); positive = offset grows), first candidate with room wins, all-pinned → innermost fallback (gesture anchors under pointer; CSS default overscroll semantics). Unit tests ×3 (prefers-innermost, handoff both directions, all-pinned fallback). DEFERRED to MWA-C-scroll: mid-momentum handoff (a fling hitting the inner boundary mid-animation splitting the remainder to the ancestor — needs timer-side delta splitting in scroll_timer.rs). STATUS: DONE
- [x] MWA-B3 Pure-Wayland clipboard — DONE (this commit). Native wl_data_device selection both directions, XWayland x11-clipboard kept as fallback only. COPY: defines.rs gains wl_data_source (opaque + listener + hand-built interface table, offer=0/destroy=1/set_actions=2, version 3); WaylandWindow.wayland_set_selection() creates the source via marshal_constructor on data_device_manager, offers text/plain;charset=utf-8 + UTF8_STRING + text/plain, adds WL_DATA_SOURCE_LISTENER, set_selection(source, last_input_serial), flush; text parked in clipboard::NATIVE_COPY, served by data_source_send (EINTR-safe full write + close), ownership cleared by data_source_cancelled (destroys source). Serial: last_input_serial captured from BOTH pointer button and keyboard_key_handler (key serial stored BEFORE the keymap guard so Ctrl+C works without prior clicks — keyboard serial was previously dropped entirely). PASTE: data_device_selection stub now stores the compositor's offer (destroys the replaced one, opcode 2); read path = own-selection short-circuit (avoids self-receive deadlock on the single-threaded loop) → receive_offer_bytes (generalized from receive_uri_list, mime param, flush-before-close pipe pattern) → XWayland fallback. clipboard.rs finds the live WaylandWindow via the main-thread linux registry. NEEDS-RUNTIME-VERIFY: wl-copy/wl-paste interop both directions in Phase D wayland smoke. DEFERRED to MWA-C-clipboard: zwp_primary_selection_v1 (middle-click paste).
- [x] MWA-B4 Touch→gesture bridge — DONE (manager half 2eab44763, backend half this commit). Manager: per-touch-id sessions (touch_sessions map, touch_down/move/up + touch_cancel_all, record_sample_for_session by id — never last_mut(); 4 unit tests incl. spread→pinch-out). Backends: wayland handle_touch_point (is_new → down vs move) / handle_touch_up (last-known pos) / handle_touch_cancel (cancel_all); X11 XI_TouchBegin/Update/End branch feeds sessions with root-coord screen positions; Windows feed_pointer touch branch (was_present → move vs down, is_up → up, raw-pixel screen pos); macOS GLView+CPUView gained magnifyWithEvent:/rotateWithEvent: → view_handlers::magnify/rotate → inject_native_gesture(Pinch{scale=1+magnification, synthesized distances}/Rotation{-degrees.to_radians() for CCW-degrees→CW-radians}) + immediate event pass via apply_activation_pass_result. NEEDS-RUNTIME-VERIFY: real two-finger pinch on a touchscreen (X11/Windows) + trackpad pinch/rotate on macOS in Phase D smoke. STATUS: DONE
- [x] MWA-B5 X11 CSD decorations — DONE. apply_motif_wm_hints (Motif bits: None=0, NoTitle=border+resize, NoControls=+title, else ALL; c_long packing for format=32) applied at creation pre-map + re-applied on runtime flags.decorations change in sync_window_state. NEEDS-RUNTIME-VERIFY (WM matrix). STATUS: DONE
- [x] MWA-B6 Wayland decorations — DONE. Decoration-mode request now honors flags: CSD-wanting/frameless windows request client_side(1), others server_side(2) (was unconditional SSD); protocol ABSENT → flags flipped to None+has_decorations so the shared injection path draws the azul titlebar (no more bare uncloseable rectangle on GNOME); configure handler now ACTS on refusal (compositor answers client_side while window expects decorations → flip to CSD + regenerate) — and its listener registration passed a pointer to the LOCAL BINDING (&mut window as *mut _), dormant-fatal, fixed to the window pointer. NEEDS-RUNTIME-VERIFY: KDE (protocol, honors SSD), GNOME (no protocol), Sway (protocol, may refuse). STATUS: DONE
- [x] MWA-B7 File drop completeness — DONE (core fbf74a73d + remainder this commit). Per-node targeting (mouse_target + Window filter mirrors); multi-file end-to-end (manager Vecs + 8 ingress sites + CallbackInfo get_*_files + API-STAGED C getters + headless FileHover/FileDrop take Vec<String>); DRAG-POSITION FRESHNESS: macOS handlers take the NSDraggingInfo.draggingLocation (converted via macos_to_azul_coords) and set cursor_position before the hit test; Windows handlers take the OLE POINTL (ScreenToClient + HiDPI → logical) via new set_drag_cursor_from_screen, and DragOver now drives a NEW handle_file_drag_moved (position + hit test + pass — it previously did nothing positional, so HoveredFile never re-targeted while the drag moved). NEEDS-RUNTIME-VERIFY: Finder→azul multi-file drop onto a nested drop target (the acceptance case), Explorer→azul drag-over re-targeting. STATUS: DONE
- [x] MWA-B8 Drag auto-scroll — DONE (this commit). auto_scroll_timer_callback: (1) accepts CursorPosition::OutOfWindow coords (the old get_position()==None self-termination killed edge-scroll the moment X11 delivered LeaveNotify under the implicit grab, or Windows fired WM_MOUSELEAVE under SetCapture — holding still past the edge stopped scrolling); (2) liveness = left_down OR is_drag_active() OR hovered-file present (OS drags have no button state on our side); (3) anchor = focused node OR deepest hovered node (new CallbackInfo::get_deepest_hovered_node — non-text drags never found a scroll container before). Triggers: TextSelectionDrag (unchanged, core) + NEW: process_window_events starts the timer on FileHover/Drag/DragStart events (idempotent handler). Windows scrollbar SetCapture sub-fix: OBSOLETE — already fixed post-audit (early-return branch now captures, with a comment narrating the audit bug). NEEDS-RUNTIME-VERIFY: X11 hold-past-edge text selection; Explorer file-drag edge scroll. TEST-DEBT: headless E2E for drag-past-edge (needs OutOfWindow simulation event — note for MWA-C-gesture). STATUS: DONE
- [x] MWA-B9 macOS titlebar per D2 — DONE (this commit). NATIVE PATH: macOS implements handle_begin_interactive_move via performWindowDragWithEvent(NSApp.currentEvent) — the begin_interactive_move plumbing already existed end-to-end for Wayland (CallbackInfo → change → PlatformWindow hook), so titlebar_drag_start now takes the native path on macOS too (OS-smooth, snap-aware, multi-monitor-correct; manual loop = X11/Windows + programmatic fallback). MANUAL-PATH FIXES: Y-flip now flips against the PRIMARY screen height (Cocoa's global origin is the primary's bottom-left; per-screen height was only right on the primary → secondary-monitor teleport) in BOTH sync_window_state apply and windowDidMove read-back, via new primary_screen_height() (also no longer skips silently when screen()==None); titlebar_drag rounds instead of truncating deltas (sub-pixel Retina drags stalled at `as i32`). DEFERRED to MWA-C-gesture/C-csd: full fractional-residual carry; restore-before-drag while maximized; 5px-threshold exemption for window drags (native path makes all three cosmetic on macOS). NEEDS-RUNTIME-VERIFY: drag across two different-scale monitors; snap zones. STATUS: DONE
- [x] MWA-B10 a11y scroll surface — DONE (this commit). ScrollManager::a11y_scroll_info (offset + max per axis, None if unscrollable); update_tree takes &ScrollManager and declares scroll_x/y + min/max + clips_children + ScrollUp/Down (max_y>0) / ScrollLeft/Right (max_x>0) / SetScrollOffset on scrollable nodes; build_node maps the never-read AccessibilityInfo.supported_actions via new map_action_to_accesskit (outbound twin of the inbound table) and declares ScrollIntoView on every content node (inbound handler implements all of these already — layout/window.rs process_accessibility_action). NEEDS-RUNTIME-VERIFY: VoiceOver rotor scroll + Orca flat-review scroll drive the container. STATUS: DONE
- [x] MWA-B11 CSD resize edges — DONE (this commit). Shared csd_resize_edge_at + CsdResizeEdge + 8px band (common/event.rs, unit-tested incl. band boundary), gated on decorations==None. Wayland: new xdg_toplevel_resize dlopen wrapper (opcode 6 "ouu") driven from handle_pointer_button BTN_LEFT edge press. X11: new begin_net_wm_moveresize (_NET_WM_MOVERESIZE ClientMessage to root, XUngrabPointer first — the implicit grab blocks the WM — mirrors the _NET_WM_STATE sender) from handle_mouse_button. Windows: WM_LBUTTONDOWN edge press → ReleaseCapture + SendMessageW(WM_NCLBUTTONDOWN, HT10..17) hands the sizing loop to the OS. macOS ROOT CAUSE: WindowDecorations::None REMOVED Resizable from the styleMask — keeping it gives native borderless edge-drag (AppKit 10.12+), no manual edges needed. Double-click-titlebar parity: OBSOLETE (titlebar_double_click Maximized toggle already wired). NEEDS-RUNTIME-VERIFY: resize on all 4 (KDE/GNOME/Xfwm/Win11/macOS). STATUS: DONE
- [x] MWA-B12 Long-press — DONE. LONG_PRESS_TIMER_ID (0x0007→0x0006) one-shot wake armed on every MouseDown at threshold+15ms (make_one_shot_pass_timer, inert self-terminating marker; invoke_expired_timers fires the event pass — a motionless hold finally evaluates detect_long_press); re-fire fixed: PWE marks the current session via new mark_current_long_press_invoked right after a LongPress event is emitted (mark_long_press_callback_invoked had ZERO callers; determination already skips callback_invoked sessions). STATUS: DONE
- [x] MWA-B13 Wayland scroll direction — DONE. handle_pointer_axis negates both axes: wl_pointer.axis is positive toward bottom/right but the raw-delta chokepoint uses the X11 convention (up=+1) that scroll_sign() normalizes from — pass-through inverted every Wayland scroll. NEEDS-RUNTIME-VERIFY: real compositor, both natural-scroll settings. STATUS: DONE
- [x] MWA-B14 macOS native Edit menu — DONE (this commit). menu::build_edit_submenu (Undo Cmd+Z / Redo Shift+Cmd+Z / Cut/Copy/Paste / Select All, standard nil-target selectors) installed from BOTH menubar builders (launch stub setup_main_menu + user-menu create_menubar_nsmenu, right after the app submenu); GLView+CPUView implement copy:/cut:/paste:/selectAll:/undo:/redo: forwarding through view_handlers::edit_command into the SAME SystemChange path as the keyboard shortcuts (target-carrying Cut/Undo/Redo require a focused node). Covers the A2 flagsChanged uncertainty: Cmd equivalents now dispatch through AppKit regardless. NEEDS-RUNTIME-VERIFY: menu items enable + fire with a focused text field; Services integration smoke. STATUS: DONE — PHASE B COMPLETE (14/14).

## Phase C — full per-manager sweep (report §5 is the checklist; a manager is DONE only when EVERY gap in its section is FIXED, OBSOLETE, or SKIPPED-with-reason here)
- [x] MWA-C-gesture — DONE (this commit). Audit §5.1 full triage: headless sessions=A4; pinch/rotate=B4; long-press double-break=B12; auto-scroll rows=B8; double/triple-click, swipe, node-DnD, pen state = already WIRED (audit-confirmed). NEW FIXES: (1) native_gesture latch — gesture.rs documented a per-cycle clear that NO desktop path performed, so a B4-injected macOS magnify (or debug-server injection) re-fired Pinch* on every later pass forever; cleared in PWE's clear-flags block (iOS/Android keep their own per-frame clears). (2) drag cancellation — cancel_drag/DeactivateDrag had zero input-side invokers: Escape-during-drag or WindowFocusOut (Alt-Tab mid-drag) now applies SystemChange::DeactivateDrag (merged into pass result). Also fixed B8 latent E0308 (.copied() on by-value Option<DomNodeId>) + B-era E0603 (macos_to_azul_coords → pub(super), used by mod.rs magnify/rotate handlers). DISPOSITIONS: Wacom pad backends → FOLLOW-UP (no call sites on any OS, needs hardware; get_pad_state honestly returns None); window-move drag-context dead outputs (get_window_drag_delta/is_window_dragging/get_window_position_from_drag — 0 callers; titlebar uses get_drag_delta_screen_incremental + begin_interactive_move per B9/D2) + ActivateWindowDrag over-firing → FOLLOW-UP API-cleanup note (harmless: context only gates Drag-event emission); Windows WM_MOUSEMOVE left-bit-only InputSample.button_state = latent [info], field unread — no action; fractional-residual carry for manual window drags → C-csd as planned. TEST-DEBT carried: headless drag/double-click E2E now unblocked by A4 (write in Phase D if time allows). STATUS: DONE
- [x] MWA-C-scroll — DONE (this commit). Audit §5.2 full triage: horizontal wheel=B1; nested target+input-handoff=B2; Wayland axis sign=B13; Windows scrollbar SetCapture + headless tick/easing = OBSOLETE (fixed post-audit). NEW FIXES: (1) mid-momentum boundary handoff (B2 deferral): scroll_timer.rs no longer kills velocity at a non-rubber-band edge — residual momentum walks find_scroll_parent to the nearest ancestor with room in the fling's direction (overscroll-behavior contain/none stops the chain at the exhausted node, matching CSS); (2) Wayland trackpad classification: axis_source/axis_stop stubs filled — finger/continuous → TrackpadContinuous (was WheelDiscrete = impulse stacking made touchpad scrolls fly), axis_stop → zero-delta TrackpadEnd via new handle_pointer_axis_stop (rubber-band spring-back; wheel-source axis_stop ignored); new WaylandWindow.current_axis_source field; (3) Windows wheel: WM_MOUSEWHEEL/HWHEEL lparam is SCREEN coords — now ScreenToClient'd (was treated as client → wrong hit-test on any window not at origin; worse than the audit's stale-hit-test [info]) AND hit-test refreshed BEFORE record_scroll_from_hit_test (macOS/X11 order); (4) keyboard scrolling without focus: default_actions arrows return ScrollFocusedContainer with no focused node, Space/Shift+Space page-scroll added (activatable-focus still wins; text inputs keep Space/arrows), event.rs consumer anchors on focused OR deepest-hovered node; (5) scrollbar arrows + track_click: handle_track_click re-runs the component hit-test (ScrollbarHitId folds buttons into Track) — Top/BottomButton line-scroll (±KEYBOARD_SCROLL_LINE_PX), track clicks honor SystemStyle.scrollbar_preferences.track_click (JumpToPosition vs PageUpDown paging toward the click vs thumb center) — pref was computed everywhere, consumed nowhere; (6) headless scrollbar interaction: both mouse-arm copies (run-loop + step()) route perform_scrollbar_hit_test/handle_scrollbar_click/handle_scrollbar_drag/release-clears (desktop pattern) — thumb drag + track jump now E2E-testable. DISPOSITIONS → FOLLOW-UPS: X11 XI2 smooth-scroll valuators (TrackpadContinuous on X11; large change — XI2 scroll classes replace core buttons 4/5 when selected, needs emulation suppression; most Linux touchpads are on Wayland now = fixed above); Windows precision-touchpad DirectManipulation/WM_POINTER pan (PTP already delivers high-res WM_MOUSEWHEEL; proper phase info needs COM DirectManipulation); OS natural-scroll pref detection (macOS/Wayland pre-apply in the OS; Windows FlipFlopWheel is per-device HID registry) — AZ_NATURAL_SCROLL env stays the hook. iOS/Android no-scroll-ingress = D10 FOLLOW-UP (already filed). NEEDS-RUNTIME-VERIFY: Wayland finger-scroll feel + spring-back on a live compositor; Windows multi-monitor wheel targeting; mid-momentum handoff feel. TEST-DEBT: headless E2E for nested-fling handoff + scrollbar drag (now unblocked). STATUS: DONE
- [x] MWA-C-a11y — DONE (this commit). Audit §5.3 full triage: Windows raise() drop = A3a; X11/Wayland update_window_focus_state = A3b; scroll-action advertisement (Action::Scroll*/scroll_x/scroll_y on scrollable nodes) = B-era a11y.rs update_tree(&ScrollManager) work; incremental tree push = A3e flush_a11y_tree_update; supported_actions mapping = B-era. NEW FIXES: (1) aria-live attribute parsing — build_node scans AriaProperty/Custom attributes for aria-live (polite/assertive/off → accesskit::Live), HTML-defined live regions previously never announced (only the explicit AccessibilityInfo.is_live_region flag worked); no API change needed (AriaProperty existed). (2) AT-SPI root window bounds — new LinuxAccessibilityAdapter::set_root_window_bounds(x,y,w,h) wrapper (+no-a11y stub; outer==inner, azul CSD is in-client) called from X11 ConfigureNotify on size/position change; had zero callers so Linux screen-coordinate queries (magnifier tracking) all resolved against origin. Wayland: N/A by design (no global position). DISPOSITIONS: a11y_dirty dead writes on win/x11/wayland/headless = harmless leftovers superseded by the A3e flush path → prune in Phase D cleanup; stale internals doc (accessibility.md) → Phase D doc touch-up; iOS/Android adapters → D10 FOLLOW-UP (already filed); headless has no OS bus by design — a11y assertions go through LayoutWindow tree + process_accessibility_action directly (TEST-DEBT: no e2e a11y coverage exists; write one headless a11y tree+action test in Phase D if time allows). NEEDS-RUNTIME-VERIFY: Orca announces live-region updates + focus moves on X11 AND Wayland; Narrator announces on Windows (raise path); VoiceOver scroll actions on macOS; root-bounds under a reparenting WM. STATUS: DONE
- [x] MWA-C-file_drop — DONE (this commit). Audit §5.4 full triage: all three majors = B7 (per-node targeting + Window filter mirrors; multi-file Vec rewrite + get_dropped_files + 8 ingress sites; headless sim + FileHover/Drop/Cancel arms); macOS/Windows stale drag position = B7 remainder (draggingLocation + POINTL + handle_file_drag_moved). NEW FIXES: (1) CallbackInfo::is_file_drag_active now also consults file_drop_manager.get_hovered_files() — an EXTERNAL Finder/Explorer hover reported false (the audit's open question; gesture/drag_drop managers only track intra-app drags); (2) stale macOS TODO(superplan g6) claiming NSDraggingDestination unwired → replaced with accurate doc (delegate IS wired on both views); (3) Wayland "<file>" hover placeholder documented at both call sites as a protocol limitation (wl_data_offer.receive is a blocking pipe round-trip the source may never answer pre-drop; real paths at drop; X11 self-heals via speculative fetch). DISPOSITIONS: deprecated NSFilenamesPboardType → Phase D touch-up note (works on all supported macOS; NSPasteboardTypeFileURL migration is cosmetic); iOS/Android UIDropInteraction/ACTION_DROP → D10 FOLLOW-UP. STATUS: DONE
- [ ] MWA-C-drag_drop (intra-app DnD in full; OS drag-SOURCE = FOLLOW-UP per user 2026-07-03; the acceptance case that MUST work is inbound Finder→azul drop, covered by MWA-B7) STATUS: TODO
- [ ] MWA-C-hover STATUS: TODO
- [ ] MWA-C-focus_cursor STATUS: TODO
- [ ] MWA-C-text_input STATUS: TODO
- [ ] MWA-C-text_edit STATUS: TODO
- [ ] MWA-C-clipboard (incl. rich-text styled_runs path documented in managers/selection.rs header; HTML clipboard format → API-STAGED if needed) STATUS: TODO
- [ ] MWA-C-undo_redo STATUS: TODO
- [ ] MWA-C-gamepad STATUS: TODO
- [ ] MWA-C-virtual_view STATUS: TODO
- [ ] MWA-C-gpu_state STATUS: TODO
- [ ] MWA-C-changeset STATUS: TODO
- [ ] MWA-C-permission (D1 FULL scope: real platform backend per report §5 fix sketch; dependency policy D9) STATUS: TODO
- [ ] MWA-C-geolocation (D1 FULL scope: real platform backend per report §5 fix sketch; dependency policy D9) STATUS: TODO
- [ ] MWA-C-biometric (D1 FULL scope: real platform backend per report §5 fix sketch; dependency policy D9) STATUS: TODO
- [ ] MWA-C-keyring (D1 FULL scope: real platform backend per report §5 fix sketch; dependency policy D9) STATUS: TODO
- [ ] MWA-C-sensors (D1 FULL scope: real platform backend per report §5 fix sketch; dependency policy D9) STATUS: TODO
- [ ] MWA-C-csd (whatever B5/B6/B9/B11 did not cover) STATUS: TODO

## Phase D — compile gate (auto-entered per D6, only when A–C exhausted)
1. `cargo check -p azul-layout -p azul-core` then `-p azul-dll --features build-dll`, then workspace — fix everything (a pile is expected; that is the deal).
2. Apply API-STAGED through the azul-doc autofix workflow (autofix commands only — never hand-curated patches), then `azul-doc codegen all` to regenerate ALL language bindings; re-check.
3. `cargo test` workspace (unit + headless E2E incl. all tests written in A–C).
4. Examples compile (workspace examples; C hello-world where cheap). Cross-compile verify: `cargo check --target x86_64-unknown-linux-gnu -p azul-dll --features build-dll` with CXX/CC/AR/LINKER per the crosscompile-vkmem memory note; windows target too if a toolchain is present (else CI).
5. Walk the NEEDS-RUNTIME-VERIFY list; record per-OS smoke results here.
6. SUCCESS CONDITION (D6): every report §5 gap FIXED / OBSOLETE / FOLLOW-UP + steps 1–4 green. Then set STATUS: DONE at the top, remove the lock, CronList → CronDelete the fix-loop cron, write a final summary + merge recommendation for the user.

## API-STAGED (api.json edits awaiting Phase D regen)
- CallbackInfo (api.json): add `get_hovered_files` / `get_dropped_files` returning StringVec (MWA-B7 multi-file drops; Rust-side methods exist, C-API wrapper + api.json entry in Phase D via autofix).
- EventType (core/src/events.rs enum, repr(C), exposed in api.json): appended `GeolocationFix`, `GeolocationError` at the END of the enum (ABI-safe append; HoverEventFilter/WindowEventFilter already had both variants in api.json). api.json itself NOT yet edited — apply via azul-doc autofix in Phase D.
- EventType + HoverEventFilter + WindowEventFilter: appended `PermissionChanged`, `BiometricResult`, `KeyringResult` at the END of all three enums (MWA-A1b; ABI-safe appends). Apply via azul-doc autofix in Phase D.

## FOLLOW-UPS (explicitly deferred)
- OS drag-SOURCE (dragging content OUT of azul into other apps: NSDraggingSource / DoDragDrop / XDND source side / wl_data_source) — deferred by user 2026-07-03. Inbound drops (Finder→azul) are IN scope (MWA-B7).
- (pending D10) iOS/Android real capability backends (GCController, Android sensors); dll/src/web wiring.
- Wacom tablet-PAD backends (express keys / touch ring: Wintab, libwacom, macOS tablet-proximity pad events) — no call sites on any OS, doc-claimed dir dll/src/desktop/extra/wacom_pad/ never existed; needs real hardware to develop against. get_pad_state stays an honest None. (MWA-C-gesture disposition 2026-07-03.)
- X11 XI2 smooth-scroll valuators (TrackpadContinuous on X11): XI2 scroll classes REPLACE core buttons 4/5 when selected on the master pointer — needs valuator delta accumulation + emulated-button suppression; sketch in MWA-C-scroll notes. (MWA-C-scroll disposition 2026-07-03.)
- Windows precision-touchpad pan phases (DirectManipulation or WM_POINTER pan config): PTP already delivers high-res WM_MOUSEWHEEL so scrolling works, but TrackpadContinuous/TrackpadEnd classification (and thus spring-back) needs COM DirectManipulation. (MWA-C-scroll disposition 2026-07-03.)
- OS natural-scroll preference detection (set_natural_scroll callers): macOS + Wayland/libinput pre-apply the preference in the OS; Windows FlipFlopWheel is a per-device HID registry key; X11 has no universal setting. AZ_NATURAL_SCROLL env stays the test hook. (MWA-C-scroll disposition 2026-07-03.)
- Window-move drag-context API cleanup: get_window_drag_delta / get_window_position_from_drag / is_window_dragging / get_window_position_at_session_start have zero callers (titlebar path uses get_drag_delta_screen_incremental + begin_interactive_move per D2/B9), and ActivateWindowDrag fires on every DragStart without a draggable ancestor (only gates Drag-event emission — harmless). Either wire a manual-window-drag consumer or prune the outputs. (MWA-C-gesture disposition 2026-07-03.)

## Session log
(one line per tick: UTC time — items touched — commits — notes)
- 2026-07-03 ~14:00 (interactive session, pre-cron) — MWA-A1a implemented (pump module, timer 0x0005, regenerate_layout surgery, geolocation EventProvider, listener flags, capability honesty) + tests; A1b split out for next tick. NOT COMPILED per rules.
- 2026-07-03 tick 1 — MWA-A1b (99b40de0e: 3 new event types ×3 enums + providers on permission/biometric/keyring, targeted permission events, in-flight counters, geolocation error channel, 100ms async cadence) + MWA-A2 (primary-modifier shortcuts, macOS word-modifier, Windows generic-VK mapping) + tests. Next: A3 output-drain sweep.
- 2026-07-03 tick 2 — MWA-A3 complete (UIA raise, AT-SPI focus state ×2 backends, incremental :hover restyle, macOS activation passes, flush_a11y_tree_update hook ×4 backends, a11y-default OBSOLETE). Cron retuned to every 5 min (job 8e26d3b1) per user. Next: A4 headless input parity.
- 2026-07-03 tick 3 — MWA-A4 (92dc5145e: headless gesture sessions in both dispatch sites, FileHover/FileDrop/FileHoverCancel simulation events, wheel/easing OBSOLETE; rust-analyzer caught a by-value-self E0308, amended). PHASE A COMPLETE (A1a/A1b/A2/A3/A4). Next: Phase B — B1 horizontal wheel.
- 2026-07-03 tick 5 — MWA-B2 (2a8672c4e: innermost-first + boundary handoff in ScrollManager, 3 unit tests; rust-analyzer caught NodeId::from_usize Option, fixed inline). Next: B3 pure-Wayland clipboard.
- 2026-07-03 tick 6 — MWA-B3 recon complete → full implementation spec written into the item (all idioms verified: receive pipe template, marshal_constructor pattern, serial storage, registry access, opcode tables). Deliberately NOT half-implemented at the tail of a long context window; next tick executes the spec in one focused go.
- 2026-07-03 tick 21 — MWA-C-a11y (this commit: aria-live AriaProperty/Custom parsing → accesskit Live; AT-SPI set_root_window_bounds wrapper + X11 ConfigureNotify call; everything else in §5.3 was already fixed by A3a/A3b/A3e/B-era — verified against audit). Next: MWA-C-file_drop.
- 2026-07-03 tick 20 — MWA-C-scroll (this commit: mid-momentum handoff up the scroll chain in scroll_timer.rs; Wayland axis_source/axis_stop → TrackpadContinuous/End; Windows wheel screen→client coords + hit-test-before-record; keyboard scroll hovered fallback + Space page-scroll; scrollbar arrows line-scroll + track_click pref consumed; headless scrollbar click/drag/release routed in both mouse-arm copies; XI2 smooth scroll + PTP DirectManipulation + natural-scroll pref → FOLLOW-UPS). Next: MWA-C-a11y.
- 2026-07-03 tick 19 — MWA-C-gesture (this commit: native_gesture PWE clear — B4-injected pinch no longer latches; Escape/blur drag cancel via DeactivateDrag; B8 .copied() E0308 + macos_to_azul_coords E0603 visibility fixed; Wacom pad + window-drag-context outputs → FOLLOW-UPS). Next: MWA-C-scroll (mid-momentum handoff).
- 2026-07-03 tick 18 — MWA-B14 (this commit: native Edit menu in both menubar builders + responder-chain selectors on both views → SystemChange path). PHASE B COMPLETE (B1-B14). Next: Phase C per-manager sweep, starting MWA-C-gesture.
- 2026-07-03 tick 17 — MWA-B12 (one-shot long-press wake + re-fire mark) + MWA-B13 (wayland axis sign). Phase B: only B14 (macOS Edit menu) remains.
- 2026-07-03 tick 16 — MWA-B11 (this commit: shared edge-band helper + native resize on all 4 backends; macOS root cause = Resizable stripped from borderless styleMask). Next: B12 long-press timer, B13 wayland scroll sign, B14 Edit menu — then Phase C.
- 2026-07-03 tick 15 — MWA-B10 (this commit: scroll surface + supported_actions declared in the accesskit tree; a11y scenario now COMPLETE end-to-end with A3's raise()/focus-state fixes). Next: B11 CSD resize edges.
- 2026-07-03 tick 14 — MWA-B9 (this commit: performWindowDragWithEvent via existing begin_interactive_move hook, primary-screen Y-flip in apply+read-back, delta rounding). Scenario (c) complete — all five user scenarios landed. Next: B10 a11y scroll surface.
- 2026-07-03 tick 13 — MWA-B8 (this commit: OutOfWindow-valid auto-scroll timer, drag/file liveness + hovered-node anchor, FileHover/Drag/DragStart triggers; Windows scrollbar capture OBSOLETE — fixed post-audit). Next: B9 macOS titlebar.
- 2026-07-03 tick 12 — MWA-B7 remainder (this commit: macOS draggingLocation + Windows POINTL freshness, DragOver re-targeting via new handle_file_drag_moved, headless multi-file sim). B7 COMPLETE. Next: B8 drag auto-scroll.
- 2026-07-03 tick 11 — MWA-B7 core (this commit: per-node targeting + Window filter mirrors + multi-file manager/ingress×8/CallbackInfo accessors + tests). Remainder spec'd in item: drag-position freshness (macOS/Windows), API staging done, headless multi-file sim. Next tick: B7 remainder → B8.
- 2026-07-03 tick 10 — MWA-B5 (X11 motif hints) + MWA-B6 (wayland decoration negotiation honors flags, protocol-absent CSD fallback, configure-refusal handling, dormant listener-ptr bug fixed). Next: B7 file-drop completeness.
- 2026-07-03 tick 9 — MWA-B4 part 2/2 (this commit: wayland/x11/windows touch handlers feed per-finger sessions; macOS magnify/rotate native injection wired into both view classes). B4 COMPLETE. Next: B5 X11 _MOTIF_WM_HINTS.
- 2026-07-03 tick 8 — MWA-B4 part 1/2 (2eab44763: per-touch-id gesture sessions in the manager + 4 unit tests; two-finger corruption of last_mut()-only recording eliminated). Backend call sites (wayland/x11/windows touch handlers + macOS magnify/rotate injection) = next tick per spec in the B4 item.
- 2026-07-03 tick 7 — MWA-B3 EXECUTED per spec (this commit): wl_data_source binding + native copy (set_selection w/ pointer+keyboard serials) + native paste (selection offer stored, receive_offer_bytes generalized) + own-selection deadlock guard + XWayland fallback retained. Next: B4 touch→gesture bridge.
- 2026-07-03 tick 4 — MWA-B1 (6e215f3dc: X11 buttons 6/7 → handle_scroll; Windows WM_MOUSEHWHEEL OBSOLETE, already fixed post-audit). USER DESIGN NOTE for B2: nested-scroll chaining lives in ScrollManager::record_scroll_from_hit_test target resolution (innermost-first + leftover-delta handoff) — NOT in WebRender (modern WR only applies ExternalScrollId offsets; chaining is the embedder's job, Gecko does it in APZ) and NOT in cpurender — one ScrollManager fix serves the WR path, the cpurender path, and headless E2E alike. Next: B2.
