# Azul Bindings — Handoff for Friday 2026-05-15

**Last session ended:** Wednesday 2026-05-13 ~22:00 CEST
**Resume on:** Friday 2026-05-15
**Plan of record:** `scripts/OVERNIGHT_PLAN_2026_05_13.md`
**State:** 141 closed/deferred items / 13 truly open. Loop is **paused** —
do NOT auto-fire on wake; wait for explicit user direction.

## Session 2 (2026-05-13) one-paragraph summary

Closed Phase H (Haskell deep polish, 9/10 items — H.2 gated on libazul
C.1), substantial Phase I (per-language native idiom integration —
Iterators / Eq / Hash / Debug / Clone landed across all 8 bindings;
Option/Result auto-unwrap in 3 dynamic-typed bindings), and Phase J
(codegen audit for hardcoded class-name dispatches — reduced from 24
sites to 1, a 96% drop, via 4 shared type-driven detectors). Hello-world
LOC across the 8 wrapper-heavy languages averages **-35% vs. session
start**; the Python LOC gap dropped from 3-4× to 1.5-2×.

## What's actionable Friday (13 open items)

Roughly sorted by effort + value:

### Quick wins / scoped (a few hours each)

None left. Everything quick already landed.

### Multi-day focused work

- **B.7.* Fortran tagged-union codegen rewrite** (~1-2 days, no
  blockers). The codegen-side fix that lets Fortran reach E2E parity
  with Java/Kotlin/C# etc. Plan describes the steps in detail
  (B.7.1–B.7.7); see also `memory/fortran_codegen_2026_05_13.md`.

- **B.5.2-5 Perl Platypus record-to-pointer spike chain** (~2-3 days).
  B.5.1 fixed the out_ptr passthrough in the invoker; B.5.2 is the
  spike to prove Platypus can write a record's bytes through an
  out-ptr. Land that, then B.5.3 (AzDom 240-byte), B.5.4 (hello-world
  rewrite), B.5.5 (AZ_DEBUG probe). See
  `memory/perl_layout_callback_2026_05_13.md`.

- **I.5.1/.2 Java/Kotlin/Scala/C# Optional\<T\> signatures** (~2-3
  days per binding). The dynamic-typed langs (Ruby/Node/Lua) got
  auto-unwrap to host nullable; the static-typed langs need per-Some-
  variant payload extraction (peek tag byte → read payload offset
  alignment-aware → construct wrapper T). Same shape as the H.4/H.5
  Haskell payload-extraction deferral.

- **I.5.6 OCaml option/result at binding boundary** (~1 day). Same
  payload-extraction blocker. Reuses the per-variant Ctypes
  infrastructure from A.1.4's `is_some` / `is_ok` work but needs to
  emit payload accessors.

### Blocked on libazul (don't pursue)

- **H.2 Haskell App.run** — gated on C.1 (libazul macOS webrender
  crash + the new resize crash documented 2026-05-13 in
  `memory/libazul_resize_crash_2026_05_13.md`).

- The Lua E2E test failure is the same family — libazul-side
  `Option::unwrap` panic in `handle_compositor_resize` (events.rs:917)
  triggered by the first mouse-moved event after the window opens.
  When libazul fixes that, the Lua E2E should recover. Several other
  macOS hello-worlds (Scala "Abort trap: 6", Pascal AZ_DEBUG crash)
  are in the same family.

## Recommended next direction

If the user wants concrete progress, **B.7 Fortran** is the
highest-impact open item that isn't downstream of a blocker. It
unlocks a full E2E binding (currently smoke-only) and the work is
well-scoped (codegen-only, no runtime side).

If the user wants smaller bites, **I.5.1 Java Optional\<T\>** could be
done as a focused day: payload extraction logic generalizes from
Java to Kotlin/C# trivially after the first.

## Memory entries written this session

All in `~/.claude/projects/-Users-fschutt-Development-azul/memory/`:

- `cobol_smoke_ceiling.md` — B.6 closed; smoke passes, full E2E needs
  user-side ENTRY paragraphs (no codegen path).
- `php_b13_smart_factory.md` — B.1.3 blocker: needs libazul-side
  `AzApp_setLayoutCallbackInvoker` OR ext-php-rs Zval-to-AzDom decode.
- `libazul_resize_crash_2026_05_13.md` — full stack trace + fix-area
  pointers for the libazul agent. Affects every macOS host-binding
  hello-world.
- `smalltalk_tonel_blocker.md` — B.9 closed; codegen emits Tonel
  syntax in a single file, Pharo wants a directory package layout.
  1-2 days focused work when demand exists.

## Loop directive

`memory/loop_directive_2026_05_13.md` is now stale — it says "READ
FIRST on wake. Pick next `[ ]`, work, mark, commit, re-schedule."

For Friday's resume: **do NOT auto-fire the loop**. The 13 remaining
items are all multi-day or blocker-downstream; an autonomous loop
firing every 20 min would churn without landing anything productive.
Wait for explicit user direction on which deep task to start.

## Commit list this session (≈40 commits)

Top of the log (most recent first):
- `015dd2e0f` plan: close I.6 / J.6 / F.1 — verification done inline
- `bf466d312` plan: session-2 snapshot — Phase H/I/J completion summary
- `4aedb27af` plan: close I.5.7/I.5.8/B.1.1/B.1.2/B.9.* — pure-doc items
- `807472a33` codegen(node): fix I.3.4 toString decode — use koffi.decode
- `2665d04d5` codegen: I.1.3/1.4/1.5 — Iterable\<T\> for Kotlin/Scala/C#
- `1d071aa71` codegen(java): I.1.2 — Vec wrappers implement Iterable\<T\>
- `a5c106324` codegen: I.5.4/5.5 — Node + Lua Option/Result auto-unwrap
- `35c29fde2` codegen(ruby): I.5.3 — auto-unwrap Option/Result at boundary
- `7fd24cc90` codegen(perl) + docs: B.5.1 out_ptr passthrough + B.4.2
- `dbbebb238` codegen(node): export _azString helper from module.exports
- `b499ab1ec` plan: B.6 COBOL smoke ceiling closed + B.1.3 PHP blocker
- `4e0ce490b` codegen: J.2 + J.5 — lift WCO factory + final hardcode audit
- `f4d27b70a` codegen(lua): J.4 — lift AzWindowCreateOptions_create hardcode
- `9fb5d9d1a` codegen: J.1 — share Button.onClick detector across 7 bindings
- `caf98178a` codegen(java): J.1 — lift Button.onClick hardcode
- `7a3785e0f` codegen: J.3 — lift s.name == "String" to TypeCategory::String
- `701f61bd1` codegen: I.1 — iterator protocols (Ruby/Node/Lua)
- `f5fa78117` codegen(ruby): I.4 Clone routing — instance method + dup alias
- `8f17bf88a` codegen: I.3 — Debug/toString routing across 7 langs
- `a4664730a` codegen: I.2.5-8 — Eq/Hash routing for Ruby/Node/Lua/OCaml
- `e41f9f3fd` codegen: I.2.1-4 — Eq/Hash routing for Java/Kotlin/Scala/C#
- `71a87ec4b` examples(haskell): H.9 + H.10 — Python-quality hello-world
- `dc2646cc9` codegen(haskell): H.8 — Show / Eq routed through C-ABI
- `9c2982bf6` codegen(haskell): H.4 / H.5 tag-byte + H.6 AzString round-trip
- `a63a8694e` codegen(haskell): H.3 AzVec\<T\> → Haskell [T]
- `7909d81cc` codegen(haskell): H.1 register\<X\>Callback helper

## E2E status (2026-05-13 21:55)

`scripts/test_all_e2e.sh` reports:
- PASS: ruby, node, scala (3/4)
- FAIL: lua (libazul-side AZ_DEBUG SIGSEGV, downstream of C.1)

Java/Kotlin/C#/OCaml not currently in the test runner; verified
buildable + smoke-passing inline through Phase H/I/J iterations.

End of handoff. Loop paused.
