# Azul — VB6

⊘ **32-bit Windows niche, out of scope.** Codegen produces `.cls` /
`.vbp` files for VB6's classic project format; the runtime is
Windows-only and 32-bit. The generated files are kept for users who
specifically need VB6 support on Windows.
