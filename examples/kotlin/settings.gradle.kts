// Hello-world Gradle settings for the Azul Kotlin example.
// Gradle 9.x requires an explicit settings file to define the
// root project name for a standalone build.
rootProject.name = "hello-world"
