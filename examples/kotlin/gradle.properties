# The generated Azul.kt is a single ~123k-line file; the Kotlin
# compiler daemon needs a larger heap than the default to compile it
# (mirrors the `kotlinc -J-Xmx4g` invocation in the README).
kotlin.daemon.jvmargs=-Xmx4g
org.gradle.jvmargs=-Xmx2g
