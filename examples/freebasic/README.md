# Azul — FreeBASIC

⊘ **No macOS-aarch64 build of FreeBASIC.** Codegen produces `.bas`
bindings but the toolchain ships only for x86_64 Linux / Windows.
The generated files are kept for documentation and porting.

See `memory/codegen_rehab_status.md`.
