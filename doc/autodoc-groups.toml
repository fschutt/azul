# azul-doc autodoc — group manifest (schema v3)
#
# Locks down the 2-tree shape of the azul book so that subagents can each
# produce a coherent batch of guide pages in parallel.
#
#   Tree 1 "getting-started" — core GUI, linear teaching order
#   Tree 2 "advanced"        — tooling, data, headless and deployment
#
# Every page belongs to a topic DIRECTORY that owns a card on the guide
# index (`events/`, `system/`, `data/`, `headless/`, `deploying/`); a page
# with no directory would be a card with nothing under it.
#
# Schema (per-group):
#   id, tree, audience[], agent_strategy, tracked_files[], tracked_globs[]
#
# Schema (per-output, inside group.outputs):
#   slug, path, title, audience (when group is dual),
#   maturity        ∈ {mature, wip, stub, draft}     default mature
#   guide_order     int — position in the linear external guide
#   topic_only      bool — routes to /guide/topics/<slug>
#   prerequisites   [slug] — earlier slugs the reader is assumed to know
#
# guide_order runs in one band per index card: the card at N0, its articles
# at N1.. (layout=80, layout/blocks=81, layout/grid=84). 10-199 is Tree 1,
# 200+ is Tree 2 — that split is what `classify_tree` reads.

[meta]
generated_from = "doc/guide/en/architecture.md"
schema_version = 3
existing_guides = ["architecture"]

# Files every agent must read before starting, to anchor on the project's
# big picture and writing conventions.
shared_context_files = [
  "doc/AUTODOC.md",                  # operational pipeline reference
  "doc/autodoc-groups.toml",         # this manifest — the page tree, prerequisites, design_docs
  "doc/guide/en/architecture.md",    # canonical architecture doc
  "scripts/ideas/architecture/ARCHITECTURE.md",         # contributor-side code map
  "api.json",                        # FFI surface (read for type names)
]

# ──────────────────────────────────────────────────────────────────────
# Tree definitions (top-level book sections)
# ──────────────────────────────────────────────────────────────────────

[[meta.trees]]
id = "getting-started"
title = "Getting Started"
audience = "external"
description = """
Linear teaching path for app authors. Each page may only use concepts \
introduced in earlier pages. Covers core GUI authoring only — DOM, \
styling, layout, text, images, widgets, events, timers, animations, \
scrolling, windowing.
"""

[[meta.trees]]
id = "advanced"
title = "Advanced"
audience = "external"
description = """
Topics beyond the core GUI loop: developer tooling (debugging, profiling, \
e2e testing), data (SQL, networking, background work), headless use \
(rendering, PDF, XML) and deployment (web, mobile, signed updates). \
Pages are mostly standalone; a reader can jump to any topic after \
finishing Tree 1.
"""

# ──────────────────────────────────────────────────────────────────────
# Writing style — applied to every page
# ──────────────────────────────────────────────────────────────────────

[meta.writing_style]
reference = "https://book.servo.org/contributing/guides/index.html — match this style. Dense, structural, zero throat-clearing. Headings carry the table of contents; prose only fills in what a heading + signature don't already say."
reader_model = "Senior engineer skimming for the answer. They have read the architecture page. They know Rust. They will Ctrl-F your headings — every H2 must be a thing they'd search for."
voice = "Declarative, second-person where unavoidable, otherwise impersonal. State facts. Don't narrate."
length_target = "Aim for the *shortest page that fully covers the topic*. Soft target 800–1500 words; hard cap 4000. Architecture / system-deep-dive pages (cascade, layout-solver, contenteditable, shell2-*, rendering-pipeline) can legitimately reach 3000–4000. If you exceed 4000, split into sub-pages rather than trim."
opening_pattern = "First sentence states a fact about the subject — never 'In this guide…' or 'This page covers…'. First code or signature within 15 lines."
code_examples = "Complete and copy-pasteable. No `…` placeholders. External pages: ```rust. Stub pages: ```rust,ignore. Hidden setup via leading `#` lines."
visuals = "Use `azul-render` fences for screenshots / slideshows. `subtitle=` for captions. `slideshow=ID` to group frames."
cross_links = "Inline links, not bibliography sections. Cite source as `core/src/foo.rs:LINE` on contributor pages."
avoid = [
  "Throat-clearing: 'In this guide…', 'Let's start by…', 'First, we will…', 'It is important to note…', 'As mentioned earlier…'",
  "Recap paragraphs at section ends ('To summarize…', 'In short…')",
  "Restating the H2 as the first sentence",
  "Marketing words: blazing-fast, powerful, robust, seamless, cutting-edge, elegant",
  "Filler adverbs: simply, just, obviously, easily, basically, essentially, actually",
  "Hedging: 'tends to', 'often', 'in most cases' — be specific or omit",
  "Long prose runs without code, signatures, tables, or lists",
  "Forward references on external pages — never use a concept before its prerequisite page introduces it",
  "Multi-paragraph docstring inside code blocks",
  "Emojis",
  "Singular 'they' for an unspecified reader — address the reader directly with 'you' / 'your'",
  "Renamed terminology: write 'master' (not 'main') for the default git branch; 'blacklist' / 'whitelist' (not 'blocklist' / 'allowlist'); 'parent' / 'child' for tree relationships. Use the established technical term.",
  "Gendered or anthropomorphizing language for code: don't say a function 'wants' something or 'knows' something — say what it does",
]
prefer = [
  "Functional H2 headings the reader will Ctrl-F for: 'Adding a callback', 'Hit-test order', 'When the layout is invalidated' — not 'Overview' or 'Introduction'",
  "Two- to three-sentence paragraphs. Break with a code block, table, or list before a 4th sentence.",
  "Bullets for alternatives, options, and enumerations. Tables for cross-products (property × variant, OS × backend).",
  "Type signatures and function signatures inline before the explanation",
  "Numbered steps for ordered procedures only — not for explanations",
  "One concept per H2. If you find yourself writing 'There are two kinds of X', that's two H2s.",
  "End each H2 at the last fact. No closing paragraph.",
]

# ──────────────────────────────────────────────────────────────────────
# Agent thinking — max effort
# ──────────────────────────────────────────────────────────────────────

[meta.agent_thinking]
mode = "max-effort"
instructions = [
  "Before writing, READ all `shared_context_files` plus the group's `tracked_files`. Do not skim — actually load the relevant sections.",
  "For each output, READ the matching `## ` sections in `doc/guide/en/reference.md` (find by source-file path).",
  "Think extended: outline each page's section structure before writing. Cross-check the outline against `prerequisites` — every concept used must already be introduced.",
  "Describe internals semantically. Backtick type, function, and field names so contributors can grep, but do NOT cite `path/to/file.rs:LINE` in prose — line numbers go stale and create maintenance churn. The authoritative source pointers belong in the page's frontmatter `tracked_files:` list.",
  "When introducing a type, link to or quote its definition. Don't paraphrase the type.",
  "After writing, RE-READ the page and check: (a) every code sample compiles in concept, (b) every cross-link target exists in the manifest, (c) frontmatter is complete and valid.",
  "If a `tracked_file` doesn't exist, note it in the page as 'planned but not yet implemented' and tag the page `maturity = stub`.",
]

# ──────────────────────────────────────────────────────────────────────
# TREE 1 — Getting Started (core GUI, linear)
# ──────────────────────────────────────────────────────────────────────

[[group]]
id = "hello-world-and-install"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/lib.rs",
  "core/src/app.rs",
  "core/src/callbacks.rs",
  "dll/src/lib.rs",
  "dll/src/python.rs",
  "examples/hello-world.rs",
  "api.json",
]
notes = "Landing page introduces the book and points at language-specific install/first-program guides. Each per-language page is self-contained and ends with a working window."

[[group.outputs]]
slug = "hello-world"
path = "doc/guide/en/hello-world.md"
title = "Hello, World"
maturity = "mature"
guide_order = 10
prerequisites = []

[[group.outputs]]
slug = "hello-world/rust"
path = "doc/guide/en/hello-world/rust.md"
title = "Hello, World — Rust"
maturity = "mature"
guide_order = 11
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "hello-world/c"
path = "doc/guide/en/hello-world/c.md"
title = "Hello, World — C"
maturity = "wip"
guide_order = 12
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "hello-world/cpp"
path = "doc/guide/en/hello-world/cpp.md"
title = "Hello, World — C++"
maturity = "wip"
guide_order = 13
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "hello-world/python"
path = "doc/guide/en/hello-world/python.md"
title = "Hello, World — Python"
maturity = "wip"
guide_order = 14
prerequisites = ["hello-world"]

# ----

[[group]]
id = "architecture-and-state"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/refany.rs",
  "core/src/dom.rs",
  "core/src/callbacks.rs",
  "doc/guide/en/architecture.md",
]
notes = "The existing architecture.md is canonical — refresh it but do not rewrite from scratch. Then write a dedicated 'Understanding RefAny' page that explains the backreference pattern with a worked example."

[[group.outputs]]
slug = "architecture"
path = "doc/guide/en/architecture.md"
title = "Architecture"
maturity = "mature"
guide_order = 40
prerequisites = ["hello-world"]

# ----

[[group]]
id = "dom-and-components"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = ["core/src/dom.rs", "core/src/id_tree.rs", "core/src/xml.rs"]
notes = "Landing covers nodes, IDs, classes, IFrame. Components page covers composing reusable Dom-returning functions and the backreference pattern in practice."

[[group.outputs]]
slug = "dom"
path = "doc/guide/en/dom.md"
title = "The DOM"
maturity = "mature"
guide_order = 50
prerequisites = []

[[group.outputs]]
slug = "architecture/components"
path = "doc/guide/en/architecture/components.md"
title = "Components"
maturity = "mature"
guide_order = 42
prerequisites = ["dom"]

# ----

[[group]]
id = "styling-tree"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = ["css/src/lib.rs", "css/src/parser.rs", "css/src/system.rs", "core/src/icon.rs", "layout/src/icon.rs"]
tracked_globs = ["css/src/props/basic/*.rs", "css/src/props/style/*.rs", "css/src/system_native_*.rs"]
design_docs = [
  "ideas/platform/SYSTEMSTYLE.md",
]
notes = "Landing introduces selectors, the cascade, and how to attach a Css to a Dom. /properties is a cheatsheet of the most-used properties grouped by category. /themes covers system-style discovery (light/dark, native colors). /text-and-fonts covers font-family, weight, alignment, and the system: keywords. /icon-packs covers registering image and font icons via IconProviderHandle."

[[group.outputs]]
slug = "styling"
path = "doc/guide/en/styling.md"
title = "Styling with CSS"
maturity = "mature"
guide_order = 70
prerequisites = ["dom"]

[[group.outputs]]
slug = "styling/properties"
path = "doc/guide/en/styling/properties.md"
title = "CSS Properties Cheatsheet"
maturity = "mature"
guide_order = 71
prerequisites = ["styling"]

[[group.outputs]]
slug = "styling/themes"
path = "doc/guide/en/styling/themes.md"
title = "System Themes"
maturity = "wip"
guide_order = 72
prerequisites = ["styling"]

[[group.outputs]]
slug = "styling/text-and-fonts"
path = "doc/guide/en/styling/text-and-fonts.md"
title = "Text and Fonts"
maturity = "wip"
guide_order = 73
prerequisites = ["styling"]

[[group.outputs]]
slug = "styling/icon-packs"
path = "doc/guide/en/styling/icon-packs.md"
title = "Icon Packs"
maturity = "wip"
guide_order = 74
prerequisites = ["styling"]

# ----

[[group]]
id = "layout-and-text"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "layout/src/lib.rs",
  "core/src/text2.rs",
  "css/src/props/basic/font.rs",
]
tracked_globs = [
  "css/src/props/layout/*.rs",
  "layout/src/text*/**/*.rs",
]
notes = "Linear teaching: simple → complex. Landing introduces all four formatting modes. Blocks covers display/position/sizing first (the default). Inline covers inline-block, the text3 engine, and fragmentainer flow. Then flex (one-axis) and grid (two-axis) as the opt-in container modes."

[[group.outputs]]
slug = "layout"
path = "doc/guide/en/layout.md"
title = "Layout"
maturity = "mature"
guide_order = 80
prerequisites = ["styling"]

[[group.outputs]]
slug = "layout/blocks"
path = "doc/guide/en/layout/blocks.md"
title = "Blocks, Sizing, and Positioning"
maturity = "mature"
guide_order = 81
prerequisites = ["layout"]

[[group.outputs]]
slug = "layout/inline"
path = "doc/guide/en/layout/inline.md"
title = "Inline, Inline-Block, and Text Flow"
maturity = "mature"
guide_order = 82
prerequisites = ["layout/blocks"]

[[group.outputs]]
slug = "layout/flex"
path = "doc/guide/en/layout/flex.md"
title = "Flexbox"
maturity = "mature"
guide_order = 83
prerequisites = ["layout"]

[[group.outputs]]
slug = "layout/grid"
path = "doc/guide/en/layout/grid.md"
title = "Grid"
maturity = "mature"
guide_order = 84
prerequisites = ["layout"]

# ----

[[group]]
id = "images-and-drawing"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = ["core/src/app_resources.rs"]
tracked_globs = [
  "core/src/svg*.rs",
  "css/src/props/basic/image.rs",
]
design_docs = [
]
notes = "Images: raster (resource handle, decoding), svg (paths, drawing), canvas-gl (custom GPU drawing via GlTexture). Design docs cover the unified DOM/CSS/inline/icon pipeline and the pack→icon resolution model."

[[group.outputs]]
slug = "images"
path = "doc/guide/en/images.md"
title = "Images and Drawing"
maturity = "wip"
guide_order = 90
prerequisites = ["dom"]

[[group.outputs]]
slug = "images/svg"
path = "doc/guide/en/images/svg.md"
title = "SVG"
maturity = "wip"
guide_order = 91
prerequisites = ["images"]

[[group.outputs]]
slug = "images/canvas-gl"
path = "doc/guide/en/images/canvas-gl.md"
title = "Canvas and GL Textures"
maturity = "wip"
guide_order = 92
prerequisites = ["images"]

# ----

[[group]]
id = "events-and-input"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/callbacks.rs",
  "core/src/events.rs",
]
tracked_globs = ["dll/src/desktop/shell2/*/event*.rs"]
design_docs = [
  "ideas/events/EVENT_ARCHITECTURE_ANALYSIS.md",
]
notes = "Events from mouse/keyboard/focus/default-actions. Design docs cover the W3C-compliant state-diffing event model and the click-event filter mismatch story."

[[group.outputs]]
slug = "events"
path = "doc/guide/en/events.md"
title = "Events and Input"
maturity = "mature"
guide_order = 60
prerequisites = ["dom"]

# ----

[[group]]
id = "text-foundation"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/selection.rs",
  "core/src/text2.rs",
  "core/src/hit_test.rs",
  "layout/src/widgets/text_input.rs",
]
design_docs = [
  "ideas/text/TEXT_SELECTION_ARCHITECTURE.md",
  "SCROLL_CURSOR_TEXT_INPUT_ARCHITECTURE.md",
]
notes = "Foundational text-editing layer between events and widgets. text-input covers the dual-path (Full DOM vs Incremental Display List) contenteditable architecture. text-selection covers the W3C Selection model — range-based, multi-node — that ranges over arbitrary DOM, not just inside a text-input widget. These are prerequisite for understanding the widgets/inputs page later in the tree."

[[group.outputs]]
slug = "events/text-input"
path = "doc/guide/en/events/text-input.md"
title = "Text Input and Contenteditable"
maturity = "wip"
guide_order = 62
prerequisites = ["events"]

[[group.outputs]]
slug = "events/text-selection"
path = "doc/guide/en/events/text-selection.md"
title = "Text Selection"
maturity = "wip"
guide_order = 63
prerequisites = ["events", "events/text-input"]

# ----

[[group]]
id = "timers-animations-scrolling"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/task.rs",
  "core/src/animation.rs",
  "core/src/scrolling.rs",
  "core/src/drag.rs",
  "layout/src/hit_test.rs",
]
design_docs = [
  "ideas/layout-css/SCROLL_ARCHITECTURE.md",
  "ideas/events/DRAG_DROP_REPORT.md",
]
notes = "Frame-driven Timer + animation runtime + scroll-and-drag user-side. Design docs cover scroll-clip vs content size (viewport-free design, virtualized IFrame scrolling) and the three-part drag architecture (visual feedback, drop-zone filtering, CSS pseudo-classes)."

[[group.outputs]]
slug = "animations/timers"
path = "doc/guide/en/animations/timers.md"
title = "Timers"
maturity = "wip"
guide_order = 101
prerequisites = ["events"]

[[group.outputs]]
slug = "animations"
path = "doc/guide/en/animations.md"
title = "Animations"
maturity = "stub"
guide_order = 100
prerequisites = ["animations/timers"]

[[group.outputs]]
slug = "animations/zombie-animations"
path = "doc/guide/en/animations/zombie-animations.md"
title = "Zombie Animations"
maturity = "wip"
guide_order = 102
prerequisites = ["animations", "dom/reconciliation"]

[[group.outputs]]
slug = "events/scrolling"
path = "doc/guide/en/events/scrolling.md"
title = "Scrolling & Drag"
maturity = "wip"
guide_order = 64
prerequisites = ["events"]

# ----

[[group]]
id = "windowing-and-widgets"
tree = "getting-started"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/window.rs",
  "core/src/window_state.rs",
  "core/src/widgets.rs",
]
tracked_globs = ["layout/src/widgets/*.rs"]
notes = "Closes Tree 1. Windowing: multi-window, menus, decorations from the user side. Widgets: the built-in widget set, picked up after the reader has seen events, text-input, text-selection."

[[group.outputs]]
slug = "system/windowing"
path = "doc/guide/en/system/windowing.md"
title = "Windows & Menus"
maturity = "mature"
guide_order = 120
prerequisites = ["hello-world", "events"]

[[group.outputs]]
slug = "widgets"
path = "doc/guide/en/widgets.md"
title = "Built-in Widgets"
maturity = "wip"
guide_order = 110
prerequisites = ["styling", "events", "events/text-input"]

[[group.outputs]]
slug = "widgets/inputs"
path = "doc/guide/en/widgets/inputs.md"
title = "Input Widgets"
maturity = "wip"
guide_order = 111
prerequisites = ["widgets", "events/text-input", "events/text-selection"]

[[group.outputs]]
slug = "widgets/structural"
path = "doc/guide/en/widgets/structural.md"
title = "Structural Widgets"
maturity = "wip"
guide_order = 112
prerequisites = ["widgets"]

# ──────────────────────────────────────────────────────────────────────
# TREE 2 — Advanced (non-core, ramps to web deployment)
# ──────────────────────────────────────────────────────────────────────

[[group]]
id = "developer-tooling"
tree = "advanced"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/debug.rs",
  "dll/src/desktop/logging.rs",
  "dll/src/desktop/shell2/common/debug_server.rs",
]
design_docs = [
  "DEBUG_API.md",
]
notes = "Three tooling pages: AZ_DEBUG flags + debug overlay, memory profiling (heap/RefAny live counts, leak hunting), e2e testing harness (drive a built app via screenshots / web target with curl). DEBUG_API.md covers the HTTP/JSON remote-control surface; INTERACTION_MATRIX.md is the 8-cell headless × debug × e2e capability matrix."

[[group.outputs]]
slug = "debugging"
path = "doc/guide/en/debugging.md"
title = "Debugging"
maturity = "wip"
guide_order = 200
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "debugging/profiling"
path = "doc/guide/en/debugging/profiling.md"
title = "Memory and Profiling"
maturity = "wip"
guide_order = 201
prerequisites = ["debugging"]

[[group.outputs]]
slug = "debugging/e2e-testing"
path = "doc/guide/en/debugging/e2e-testing.md"
title = "End-to-End Testing"
maturity = "wip"
guide_order = 202
prerequisites = ["debugging"]

# ----

[[group]]
id = "io-and-system-services"
tree = "advanced"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/xml.rs",
  "layout/src/xml/mod.rs",
  "dll/src/desktop/dialogs.rs",
]
tracked_globs = ["dll/src/desktop/shell2/*/clipboard*.rs"]
notes = "User-side I/O: standalone XML parser (works without GUI), file dialogs (open/save/folder), clipboard (text/image/custom)."

[[group.outputs]]
slug = "headless/xml"
path = "doc/guide/en/headless/xml.md"
title = "XML Parsing"
maturity = "wip"
guide_order = 222
prerequisites = []

[[group.outputs]]
slug = "system/file-dialogs"
path = "doc/guide/en/system/file-dialogs.md"
title = "File Dialogs"
maturity = "wip"
guide_order = 121
prerequisites = ["events"]

[[group.outputs]]
slug = "system/clipboard"
path = "doc/guide/en/system/clipboard.md"
title = "Clipboard"
maturity = "wip"
guide_order = 122
prerequisites = ["events"]

# ----

[[group]]
id = "async-and-networking"
tree = "advanced"
audience = ["external"]
agent_strategy = "split"
tracked_files = ["core/src/task.rs"]
notes = "Background tasks (Threads, Timer-based polling, async-without-runtime). Networking will become its own concern when AzTcp/AzUdp land — for now document what you can do today via Threads, with explicit notes on what's coming."

[[group.outputs]]
slug = "data/background-tasks"
path = "doc/guide/en/data/background-tasks.md"
title = "Background Tasks"
maturity = "wip"
guide_order = 212
prerequisites = ["animations/timers"]

[[group.outputs]]
slug = "data/networking"
path = "doc/guide/en/data/networking.md"
title = "Networking"
maturity = "stub"
guide_order = 211
prerequisites = ["data/background-tasks"]

# ----

[[group]]
id = "accessibility-and-headless"
tree = "advanced"
audience = ["external"]
agent_strategy = "split"
tracked_files = [
  "core/src/a11y.rs",
  "layout/src/managers/a11y.rs",
  "layout/src/cpurender.rs",
  "dll/src/desktop/native_screenshot.rs",
]
tracked_globs = ["dll/src/desktop/shell2/*/accessibility*.rs"]
notes = "Accessibility from the app dev's POV (roles, labels, screen-reader testing). Headless rendering: cpurender, native screenshots, server-side use without a window manager."

[[group.outputs]]
slug = "system/accessibility"
path = "doc/guide/en/system/accessibility.md"
title = "Accessibility"
maturity = "wip"
guide_order = 125
prerequisites = ["dom"]

[[group.outputs]]
slug = "headless/rendering"
path = "doc/guide/en/headless/rendering.md"
title = "Headless Rendering"
maturity = "wip"
guide_order = 220
prerequisites = ["dom"]

# ----

# The `codegen-and-bindings` group is gone. Its first page (`code-generation`)
# was deleted as internal-only, and the four planned `bindings/<lang>` pages
# repeat what the 28 `hello-world/<lang>` pages already carry: install, link,
# first window. Per-language setup belongs there, not in a second tree of it.

[[group]]
id = "deploying"
tree = "advanced"
audience = ["external"]
agent_strategy = "split"
tracked_files = ["dll/src/web/mod.rs", "dll/src/web/transpiler.rs", "dll/src/web/server.rs", "layout/src/updater.rs", "scripts/sign-release.sh", "layout/src/telemetry/mod.rs", "layout/src/telemetry/config.rs", "layout/src/telemetry/metrics.rs", "dll/src/desktop/app.rs"]
tracked_globs = ["dll/src/web/*.rs", "layout/src/telemetry/*.rs"]
notes = "Everything between a working binary and a shipped one. web: WASM bundle, SSR, transpiler limits. mobile: .apk/.ipa from any OS, no Xcode or Android Studio. signing-updates: the release side of the self-updater (manifest v1, the root/signing key chain, scripts/sign-release.sh, the local-HTTP test recipe). observability: consent tiers, the OTLP metric/log client, crash dumps, and the collector they need."

[[group.outputs]]
slug = "deploying/web"
path = "doc/guide/en/deploying/web.md"
title = "Web"
maturity = "wip"
guide_order = 230
prerequisites = ["hello-world", "architecture"]

[[group.outputs]]
slug = "deploying/mobile"
path = "doc/guide/en/deploying/mobile.md"
title = "Mobile (iOS & Android)"
maturity = "wip"
guide_order = 231
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "deploying/signing-updates"
path = "doc/guide/en/deploying/signing-updates.md"
title = "Signed Updates"
maturity = "wip"
guide_order = 232
prerequisites = ["hello-world"]

[[group.outputs]]
slug = "deploying/observability"
path = "doc/guide/en/deploying/observability.md"
title = "Observability"
maturity = "wip"
guide_order = 233
prerequisites = ["hello-world", "deploying/signing-updates"]
