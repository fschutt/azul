[
  {
    "name": "hello_world_counter_increment",
    "description": "Assert the counter text actually renders (blank-window guard), then click and re-snapshot. NOTE: post-click assert_text reads a stale DOM (runner bug, noted 2026-07-04); increments are verified via the AZ_DEBUG click probe.",
    "steps": [
      {
        "op": "assert_text",
        "selector": "body > div",
        "expected": "5"
      },
      {
        "op": "get_state"
      },
      {
        "op": "click",
        "text": "Increase counter"
      },
      {
        "op": "wait",
        "ms": 300
      },
      {
        "op": "get_state"
      }
    ]
  }
]
