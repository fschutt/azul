//! Basic CSS properties and their parsing code

pub mod angle;
pub mod animation;
pub mod color;
pub mod direction;
pub mod font;
pub mod geometry;

pub use self::{angle::*, animation::*, color::*, direction::*, font::*, geometry::*};
